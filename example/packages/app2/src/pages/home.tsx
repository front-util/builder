export default function Home() {
    return (
        <section>
            <h1>App2 — Home</h1>
            <p>This is the remote application (app2). It can run standalone or be consumed by a host via Module Federation.</p>
        </section>
    );
}
